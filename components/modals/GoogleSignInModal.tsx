/**
 * -  should show a Sign in with google button
 * -  clicking the button should close the modal and sign in the user
 */

import React from 'react';
import { View } from 'react-native';

// TODO: google sign-in modal

const GoogleSignInModal = () => {
  return <View />;
};

export default GoogleSignInModal;
