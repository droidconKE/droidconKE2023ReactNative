/**
 * - each speaker card should display Image of speaker, name, description, and session buttons
 * - clicking session button should navigate to [session] page
 */

import React from 'react';
import { View } from 'react-native';

// TODO: Implement speaker card.

const SpeakerCard = () => {
  return <View />;
};

export default SpeakerCard;
